# Задание 1, объяснить результаты интерпретирования следующих двух программ.

i = sum = 0
while i <= 0:
    sum += i
    i = i+1
print(sum)

"""Данная программа использует цикл while. В самом начале кода, сказано, что
переменная i и переменная sum равны нулю. В цикле же сказано, что пока i<= 4,
то код выполняется. В каждой итерации цикла значение переменной i прибавляется
к переменной sum, и затем значение i увеличивается на 1.
После выполнения цикла программа выводит значение переменной sum.
Пройдемся по порядку, На первой итерации i равно 0, sum равно 0,
и sum увеличивается на 0. Теперь i увеличивается на 1.
На второй итерации i равно 1, sum равно 1 (0 + 1), и sum увеличивается на 1. Теперь i увеличивается на 1.
На третьей итерации i равно 2, sum равно 3 (1 + 2), и sum увеличивается на 2. Теперь i увеличивается на 1.
На четвёртой итерации i равно 3, sum равно 6 (3 + 3), и sum увеличивается на 3. Теперь i увеличивается на 1.
На пятой итерации i равно 4, sum равно 10 (6 + 4), и sum увеличивается на 4. Теперь i увеличивается на 1.
На шестой итерации условие i <= 4 больше не выполняется, и цикл завершается.
В итоге программа выводит значение sum, которое равно 10."""
"""
for char in 'PYTHON STRING':
  if char == ' ':
      break
  print(char, end='')
  if char == 'T':
      continue
  print('*', end='')"""
"""Данная программа выполняет итерацию по строку 'PYTHON STRING' с использованием цикла for. Внутри цикла
также проверяются 2 условия:
1. Если текущий символ в строке равен пробелу, то программа останавливается с помощью оператора break и на
экран выводится результат, который был получен до того, как программа дошла до пробела.
2. Второе условие проверяет, что если текущий символ равен букве 'O', то выполнение текущей итерации прерывается 
с использованием оператора continue, а затем следующим символом становится *.
В результате, мы получим, 5 итерации, а на экран будет выведено 'PYTH*', потому что итератор прошелся по
циклу до тех пор, пока не встретил символ 'O' и прекратил дальнейшую итерацию."""

# Задание 2

# Пункт 1
# Запрашиваем у пользователя число
"""
number = int(input("Введите число: "))

# Используем оператор if...elif...else для вывода сообщения
if number > 0:
    print("Число положительное")
elif number == 0:
    print("Число равно нулю")
else:
    print("Число отрицательное")# Пункт 2

# Список
fruits = ["яблоко", "банан", "апельсин", "яблоко", "банан", "киви"]

# Словарь
fruit_counts = {"яблоко": 0, "банан": 0, "апельсин": 0, "киви": 0}

# Подсчет повторений с помощью цикла for
for fruit in fruits:
    fruit_counts[fruit] += 1

# Вывод результатов
for fruit, count in fruit_counts.items():
    print(f"Фрукт {fruit} встречается {count} раз(а)")


# Пункт 3
# Определяем lambda-функцию для вычисления площади квадрата. Lambda-функция принимает х в качестве аргумента.
square_area = lambda x: x ** 2

# Применяем lambda-функцию
side_length = 5
area = square_area(side_length)

print(f"Площадь квадрата со стороной {side_length} равна {area}")
"""
# Задание 3
# Список товаров
import functions
items = []

while True:
  print("\n--- Меню ---")
  print("1. Вывести список товаров")
  print("2. Добавить товар")
  print("3. Удалить товар")
  print("4. Выход")

  choice = int(input("Выберите пункт меню: "))

  if choice == 1:
    functions.show_items(items)
  elif choice == 2:
    functions.add_item(items)
  elif choice == 3:
    while True:
      print("1. Удалить по порядковому номеру")
      print("2. Удалить по названию")
      option = int(input("Выберите вариант: "))
      if option == 1:
        functions.remove_item_by_index(items)
        break
      elif option == 2:
        functions.remove_item_by_name(items)
        break
      else:
        print("Неверный выбор.")
  elif choice == 4:
    break
  else:
    print("Неверный номер пункта.")
print("Спасибо за использование!")



